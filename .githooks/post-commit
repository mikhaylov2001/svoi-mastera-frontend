#!/bin/sh
# После каждого коммита — автоматический push (ветка с upstream или первый push -u).
branch=$(git rev-parse --abbrev-ref HEAD)
if git rev-parse --verify "@{u}" >/dev/null 2>&1; then
  exec git push
fi
exec git push -u origin "$branch"
